<UserControl x:Class="SFC.Gate.Views.Settings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SFC.Gate.Views"
             xmlns:vm="clr-namespace:SFC.Gate.ViewModels"
             xmlns:cnf="clr-namespace:SFC.Gate.Configurations;assembly=SFC.Gate.Core"
             DataContext="{Binding Source={x:Static vm:Settings.Instance}}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TabControl>
            <TabItem Header="General">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <CheckBox Grid.Column="0" Grid.Row="0" Margin="4"
                              IsChecked="{Binding Source={x:Static cnf:Config.General},Path=ConfirmExit}"
                              Content="Confirm Exit"/>
                    <TextBlock Grid.Column="1" Grid.Row="0" Margin="4" FontStyle="Italic" Opacity="0.7"
                               Text="If checked, asks for confirmation when the application is closed."/>
                </Grid>
            </TabItem>
            <TabItem Header="RFID">
                <StackPanel>
                    <GroupBox Header="Scanner">
                        <StackPanel>
                            <TextBlock Text="{Binding RegisteredScanner}"/>
                            <Button Content="Register Scanner" Command="{Binding RegisterCommand}"
                                    Focusable="False" KeyboardNavigation.IsTabStop="False"/>
                            <TextBlock Text="{Binding ScanTest}"/>
                        </StackPanel>
                    </GroupBox>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Content="Always Scan"/>
                        <TextBlock Text="Uncheck this to allow scanning of RFID Tags on Guard Mode only." TextWrapping="Wrap"
                                   FontStyle="Italic" Margin="10,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
